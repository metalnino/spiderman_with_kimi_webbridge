# 总验收清单（你只看这个 + 当前阶段）

## 使用方式

1. 打开对应 `phases/Px.md`  
2. 开发/测试勾选是否齐全  
3. 下方「验收人确认」由你勾选  

## 跨阶段总表

| 阶段 | 开发完成 | 测试完成 | 验收人（你）确认 | 日期 |
|------|----------|----------|------------------|------|
| P0 能持续转 | [x] | [x] | [ ] | 2026-08-09 Agent |
| P1 能信能筛 | [x] | [x] | [ ] | 2026-08-09 Agent |
| P2 能运营 | [x] | [x] | [ ] | 2026-08-09 Agent |
| P3 扩展 | [x] | [x] | [ ] | 2026-08-09 Agent |
| P4 展现层（详情抽屉/折叠/阶段时间线/回填） | [x] | [x] | [ ] | 2026-08-2x Agent |
| P5 岗位可信性 + 原发寻址一期 | [x] | [x] | [ ] | 2026-08-23 Agent |
| P6 管道接线 + 岗位分层 | [x] | [x] | [ ] | 2026-08-24 Agent |
| P7 召回自证 + 可投标线索口径 | [x] | [x] | [ ] | 2026-08-24 Agent |

## P7 专项验收（召回自证）

- [ ] 每次出勤后 crawl_runs.note 含 `wm_new/wm_total/pages`；简报 coverage 节每站水位推进可见
- [ ] ccgp 不再永远只扫第一页：默认最多 6 页，整页已见即停（`SPIDER_CCGP_MAX_PAGES` 可调）
- [ ] 台账线索工作台默认「仅可投标线索」（实测 23 条可投标 / 60 总量），取消勾选可见全量含历史结果
- [ ] ccgp 回溯作业 `scripts/jobs/backfill_ccgp_window.py` 断点续扫（当前 ccgp 频控阻塞，解封后执行并核验召回增量）
- [ ] `run_tests.py` failures=0 errors=0（159 用例）

## P4 专项验收（台账 http://127.0.0.1:8765/ 线索工作台）

- [ ] 列表有「阶段」筛选；每行标题旁有阶段标签（招标公告/候选人公示/结果公告…）
- [ ] 采招网×江苏站的同题公告折叠为一条「N源」，点「各源▾」可见各源链接
- [ ] 点标题弹出详情抽屉：字段按非空展示，详情/原文可点，抽屉内改状态/备注/已读即时生效
- [ ] 抽屉内「项目时间线」按阶段串联同项目公告（如招商积余南京 招标→候选人→结果）
- [ ] 抽屉「按需回填」：ccgp/采招/江苏/cebpub 分别如实返回字段/摘要/失败原因；回填成功的摘要与附件下载可见
- [ ] CRM 主体计数按折叠去重（招商商管 8、招商积余南京公司 3）
- [ ] CSV 导出含「阶段」列；`python scripts/run_tests.py` failures=0 errors=0

## P5 专项验收（岗位可信性 + 原发寻址）

- [ ] `reports/collector-report.json` 不再被测试/手动小运行污染：每次运行在 `reports/history/` 留档，`reports/briefing.jsonl` 每轮一行
- [ ] 简报含任务书到期提醒（当前窗口 2026-08-31 到期，剩 ≤14 天时提醒）
- [ ] 详情抽屉对聚合站行显示「原发」链接与原发来源（ggzy 行→上海/项目编号/金额已回填；jsggzy 行→苏州市公共资源交易中心+ccgp-jiangsu 链接；采招招商系→招商局平台）
- [ ] `python -m unittest tests.test_p5` 11 例全绿；全量 `run_tests.py` failures=0 errors=0

## P6 专项验收（管道接线 + 岗位分层）

- [ ] 采集员每轮出勤自动写 `handoffs/collector/latest.json`（items 与契约 output 同构）+ 按 runId 归档
- [ ] 解析员 `scripts/parser_from_handoff.py` 读采集交接物→筛有附件条目→解析→写 `handoffs/parser/latest.json` + `reports/parser-report.json`
- [ ] 端到端证据：真实江苏公告（医院绿植租赁）→ 5460 字 .doc → parser 需求矩阵 conf=0.8、缺失项如实 unresolved（evidence：parser_employee/handoffs/parser/latest.json）
- [ ] 总设计师 registry：parser=current/v1.1.0/解析工作区；parser 契约 v1.1.0 + MIGRATION 登记；管道契约文档含「四·五交接机制」
- [ ] 三区测试全绿：collector `run_tests.py` failures=0、parser 18 用例 OK

## 一票否决（任一出现则验收不通过）

- 密码/`.env` 进了 git  
- 中文入库乱码  
- 重复跑任务导致 notices 无脑翻倍  
- 文档勾了但命令跑不通  

## 自测命令

```
python scripts/run_tests.py
```

要求：failures=0 errors=0（真实外网抓取用例默认 skipped=3 属正常；需要时 `SPIDER_LIVE_TESTS=1` 再跑）。

台账（本机只读 API）抽查：

```
python scripts/jobs/serve_ledger.py
# 打开 http://127.0.0.1:8765/ 看监控/增量/筛选是否随库更新
```
