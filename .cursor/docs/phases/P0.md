# P0 — 能持续转

## 目标

四站增量可跑、写入 MySQL、有任务记录、有最简增量列表。你能看到「跑没跑、进没进库」。

## 开发任务

- [x] 按站适配器骨架（cebpub / chinabidding / ggzy / ccgp）+ 统一字段映射  
- [x] 反爬：按 `anti_bot.md`（暖会话钩子 + HTTP 限速；Cookie 复用预留）  
- [x] `notices` upsert + `crawl_runs` 写入  
- [x] 增量任务入口命令（`python scripts/jobs/run_incremental.py`）  
- [x] 最简增量列表页（`data/web/incremental.html`；NEW=48h）  
- [x] 更新 `config/sources.json` 与入口说明  

## 测试任务（必须按 `testing/开发测试流程.md`）

- [x] T1 冒烟：DB 连通 + 中文 OK  
- [x] T2 单站：四站各小样本至少跑通（失败需写阻塞原因，不得空勾）  
- [x] T3 增量：同一任务连跑两次，无翻倍脏数据  
- [x] T4 降级：至少一站验证「失败有记录、进程正常结束」  
- [x] T5 列表：能看到新增/入库数据  

## 入口命令

```
# 连通
python scripts/verify_db_cn.py

# 单站
python scripts/jobs/run_one_source.py ggzy --keywords 绿植租摆 --pages 1

# 四站增量
python scripts/jobs/run_incremental.py --pages 1

# 列表
python scripts/jobs/build_incremental_list.py
# 打开 data/web/incremental.html

# 自测摘要
python scripts/jobs/p0_selftest.py
```

## 测试记录（2026-08-09 Agent 自测）

| 日期 | 项 | 结果 | 备注 |
|------|----|------|------|
| 2026-08-09 | T1 | PASS | verify_db_cn 中文往返 OK |
| 2026-08-09 | T2-cebpub | PASS | run_id=3 attempted=20 |
| 2026-08-09 | T2-chinabidding | PASS | run_id=2 attempted=15 |
| 2026-08-09 | T2-ggzy | PASS | run_id=1 attempted=20 |
| 2026-08-09 | T2-ccgp | PASS | run_id=4 attempted=20（HTTP 未频控） |
| 2026-08-09 | T3 | PASS | ggzy 二次跑后仍 20 条，未翻倍 |
| 2026-08-09 | T4 | PASS | crawl_runs id=6 status=failed note=t4_simulated_rate_limit |
| 2026-08-09 | T5 | PASS | incremental.html 已生成；库合计 73 条 |

摘要：`data/web/p0_selftest.json`

## 完成标准（你只看这里）

- [x] 上方「开发任务」全勾  
- [x] 上方「测试任务」全勾（或阻塞项有文字原因且你认可）  
- [ ] `testing/验收清单.md` 中 P0「验收人确认」列（仅你勾）  

## 非目标（P0 不做）

完整级联筛选、词库 UI、AI 钩子、验证码队列页面、江苏站。
