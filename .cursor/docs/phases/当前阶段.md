# 当前阶段

**阶段：P0～P3 开发与自测已完成；台账已升级为本机只读 API；六站全开（2026-08-20 定稿口径）**

- P0：`phases/P0.md`
- P1：`phases/P1.md`
- P2：`phases/P2.md`
- P3：`phases/P3.md`（江苏站 jsggzy / 采招 Cookie 详情 / CRM）

## 运行口径（2026-08-20 起，长期）

- **NAS 已弃用，本机为唯一运行点**；六站全开：
  ccgp / chinabidding / ggzy / jsggzy = HTTP；cebpub = Playwright 无头；jiangsu_zhaobiao = WebBridge 真浏览器。
- 员工入口：`python scripts/collector_run.py`（吃契约 input → 契约 output + reports/collector-report.json）。
- 桥服务：`python scripts/webbridge_server.py`（127.0.0.1:10086，Chrome 的 Kimi WebBridge 扩展自动连；详见 `webbridge_bridge.md`）。
- 定时：Windows 任务 SpidermanCollector（8/12/18/22）+ SpidermanWebBridge（登录自启）；
  手工等价命令 `python crawl/scheduler.py` 不可用（循环版），直接跑 `scripts/collector_run.py`。

台账日常用法：

```
python scripts/jobs/serve_ledger.py
# 浏览器打开 http://127.0.0.1:8765/
```

验证码同事划：

```
python scripts/jobs/solve_captcha.py list
python scripts/jobs/solve_captcha.py open --id <id>
# 浏览器划完
python scripts/jobs/solve_captcha.py done --id <id>
```

总验收：`testing/验收清单.md`  
测试命令：`python scripts/run_tests.py`

你只需在验收清单勾选「验收人确认」。
