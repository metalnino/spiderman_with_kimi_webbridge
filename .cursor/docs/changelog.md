# 过程决策简记

| 日期 | 决策 |
|------|------|
| 2026-08-09 | 四站固定；江苏搁置；MySQL utf8mb4；HTTP主 WebBridge辅 |
| 2026-08-09 | 建立 `.cursor/docs` 为过程文档唯一源；验收看阶段勾选 |
| 2026-08-09 | 仓库：https://github.com/metalnino/spiderman_with_kimi_webbridge |
| 2026-08-09 | P0 落地：crawl 包四站增量入库 + incremental.html；T1–T5 自测通过（库约 73 条） |
| 2026-08-09 | P1/P2：清洗/级联看板/词库/监控/AI钩子/人工标记/验证码待办；unittest 14/14 |
| 2026-08-09 | P3：jsggzy（全国 ggzy 江苏切片）+ 采招 Cookie 详情可选 + CRM/entities；unittest 18/18 |
| 2026-08-09 | 台账改为本机只读 API（127.0.0.1:8765）+ ledger_app 壳页；静态 dashboard 作离线快照 |
| 2026-08-09 | 验证码：本机同事划（打开/已解决）+ Cookie 落盘回灌；台账仅放行 captcha POST |
| 2026-08-09 | NAS Docker：爬虫+台账容器（MySQL 沿用）；compose 暴露 8765 |
| 2026-08-11 | 接入江苏招标网 jiangsu_zhaobiao（账号登录可选+滑块人工；列表 Dqsearch） |
| 2026-08-14 | 修复主入口 ImportError（build_incremental_html→list）；网络测试 SPIDER_LIVE_TESTS=1 门控；http 4xx/5xx/网络错误分类；关键词上限可配(默认不限)；验证码待办去重；6 站配置/看板同步；补根 README；unittest 26/26 |
| 2026-08-14 | 详情字段抓取起步：crawl/detail.py 新增 ccgp 详情 HTTP 解析（金额/招标人/代理/项目编号）并接入 runner 回填；探针确认 ggzy=JS壳、jiangsu=Cloudflare、cebpub=验证码、chinabidding=登录墙（待接入）；unittest 27/27 |
| 2026-08-16 | 两路合并并 NAS 部署：接入 jiangsu HTTP 521 的 __jsl_clearance_s 纯 HTTP 解；定时改固定时刻 8/12/18/22 东八区；CRM 主体规范化全国合并(uk_name)；SSH 重建镜像验证 healthy |
| 2026-08-16 | 台账后端换 FastAPI（api.py + uvicorn，契约不变/自动 /docs）；源站精简为 4 站（停用 ggzy/jsggzy，代码保留）；Dockerfile 用清华 PyPI 镜像 |
| 2026-08-17 | 本机并行重跑(绿植租摆/8城/7-8月)：ccgp 可用但 8城+7-8月交集极少；加发布时间范围过滤；cebpub Playwright 渲染+提取已通、关键词过滤未通(加密搜索 searchName 未传)；chinabidding info_search 超时；jiangsu 521(JSL 未解)。以上为待修项 |
| 2026-08-18 | 六站全部攻克定稿：jiangsu WebBridge 真浏览器(JSL 两阶段) 11 条入库；cebpub Playwright performSearchRequest 5 条入库（见 anti_bot_lessons.html）；NAS 此后弃用，本机为唯一运行点 |
| 2026-08-19 | 员工化套壳：招标采集员 collector/v1.0.0 外壳四层 + 契约测试；配置层 keywords/platforms/filters.json；观测报告 reports/collector-report.json；内核仅 2 处增量 seam |
| 2026-08-20 | 六站全开（长期口径）：platforms.json 全 enabled + 路由(ccgp/chinabidding/ggzy/jsggzy=HTTP，cebpub=Playwright，jiangsu=WebBridge)；内置桥服务端 scripts/webbridge_server.py（标准库，Chrome 扩展自动连）；scheduler 支持 CRAWL_MODE=collector；本机定时任务 SpidermanCollector(8/12/18/22)+SpidermanWebBridge(登录启动) |
| 2026-08-20 | 采集员契约对齐 collector/v1.2.0：publishTime 空串→null；新内核 crawl/tenderfile.py（详情抓取+附件下载+PDF/DOCX/TXT 正文清洗+summary）；output 新增 tenderFile（可空）；观测第 7 指标 detail_fetch_success_rate；测试 47 契约用例 + 91 全量回归；ccgp 附件真下载真提取（415KB doc→47k 字）；registry collector → current |
| 2026-08-20 | tenderFile 详情抓取全平台路由（v3.1）：ggzy/jsggzy 正文 b 页 HTTP SSR 直取（a→b 纯函数转换）；chinabidding(405 WAF)/jiangsu(521) 走 WebBridge 真浏览器详情（摘要可达，附件登录墙如实 null）；cebpub vaptcha 人工验证码阻塞如实跳过；每平台/全局限额护栏；实测 3 站详情摘要 6/6 填充；测试 53 契约 + 97 全量回归 |
| 2026-08-20 | 江苏账号登录+会员附件真下载（v3.2）：桥内自动登录（tianai 滑块人工一次、会话长期复用）；导出登录 Cookie 走纯 HTTP 下 bidFiledown.jsp 附件（魔数嗅探判型：pdf/docx/旧版 .doc，HTML 拒绝）；旧版 WPS .doc 正文提取（分段表解析+字节打捞 utf-16le）；实测 .doc 5460 字、.pdf 1256 字全提取；测试 55 契约 + 99 全量回归 |
| 2026-08-22 | 江苏登录数字验证码方案落地并搁置（v3.3）：定位会员登录走 /common/img.jsp 数字验证码（60x20 粘连图）；实现 HTTP 验证码登录全流程（取图→OCR→loginPost(loginType=0)→会员中心 homePageUc.do 真实验证态→Cookie 落盘，限 3 次）；OCR 双后端 crawl/captcha_ocr.py：DeepSeek 多模态 deepseek-v4-flash-vision-exp（OpenAI 兼容，配置位就绪待 key）+ 确定性识别（渲染数字回读自测过）；确定性识别对真站图实测连续错误→按指示搁置，OCR 未配置时跳过 HTTP 尝试不空耗，待办已登记；测试 61 契约 + 105 全量回归 |
| 2026-08-22 | DeepSeek 多模态验证码识别启用、登录打通（v3.4）：数字员工专用 DeepSeek 官方 key 明文配置 config/ocr_api.json（gitignored）；学官方 vision 文档修正（思考模型 max_tokens 2000、content 空回退 reasoning）；识别增强（三视图预处理 6x/二值/反相 + 双读一致性投票）；修正 loginType=userId（原 "0" 被服务端静默拒绝）、跟随 SSO loginSuccess 回跳、同图大小写变体重试；真机多次登录成功（会员中心验证通过、Cookie 落盘、待办关闭）；遗留：SSO 跨子域传播最后一环未通（江苏站静态页仍未识别登录态，桥内真实登录已覆盖采集）；测试 62 契约 + 106 全量回归 |
| 2026-08-22 | cebpub 详情能力开发（v3.5a）：反出 SPA app.js 加密——DES-ECB-PKCS7 key=1qaz@wsx3e(前8字节)，实现 cebpub_des_decrypt；附件接口 getBulletinAttachmentUrl?bulletinId&interfaceSource=0 已调通（解密后 JSON）；getBulletin 403 解密=「未登录」；DETAIL_MODES cebpub→bridge_vaptcha：桥内尝试、vaptcha 未过如实报并登记待办、人工一次后附件自动可下；测试 64 契约 + 108 全量回归 |
| 2026-08-22 | 完成度审计收口（v3.5）：修 cebpub Playwright 列表缺 detail_url（showDetails 末段 32hex=uuid，真机验证）；江苏桥会话过期自动重连——桥内 AI OCR 登录（页内 IIFE fetch 验证码→DeepSeek 识别→#zh 切账号 tab→IIFE 提交，真机登录成功跳用户中心）；修桥 evaluate 语义（async 函数体不执行→改 IIFE Promise）；OCR 双读不一致信首读（靠服务端反馈闭环）；六站全链路收口，测试 65 契约 + 109 全量回归 |
| 2026-08-22 | P4 展现层对齐开工：需求对齐（详情抽屉/跨站视图折叠/回填分级）落 phases/P4.md；新增**公告阶段识别与项目时间线**维度（用户补充需求：招采全流程数据都要保留并按时段分割，为后续做标书提供历史结果）——crawl/stage.py 9 阶段规则（标题关键词+源站编码映射）+ project_key=sha1(city|核心名，批次词保留）；入库即算（models/db_store）+ 存量回填 job；迁移增 notice_stage/stage_rank/project_key/project_name/summary/tenderfile_path/detail_status + 2 索引；实测 50 行分布 bidding17/result19/candidate7/other4/change2/terminated1、12 个项目时间线组；测试 120 全量回归（新增 stage 11 例） |
| 2026-08-22 | 定时任务口径调整（用户指令）：SpidermanCollector 触发器改为每天 **11:00 + 22:00**（删除 8/12/18 三个）；仅本机运行——动作=本机 python scripts/collector_run.py（工作目录=爬虫工作区，全站增量接上次运行点），本机无任何任务指向 NAS（NAS 已弃用）；SpidermanWebBridge 登录自启桥服务不变 |
| 2026-08-22 | NAS 停采集、留展示（用户发现误停 Web 展示后修正）：① scripts/nas_stop_deploy.py——SSH 查态→compose down→crontab 备份注释（NAS 无 crontab 条目）；② 误停 spiderman_ledger（Web 8765+内置采集 cron 同容器）→ scripts/nas_restore_ledger.py——docker_entrypoint.sh 支持 CRAWL_CRON_HOURS=off（只跑 ledger 不跑采集，修 exec sh -c wait 退出码 127 重启循环），scheduler.parse_slots 支持 off/none/disabled→[]，compose 环境改 off，重建容器；复核 8765 /api/health 200、日志「crawl disabled — ledger only」；最终形态：**采集=本机 11/22，展示=NAS 8765（同 MySQL）** |
| 2026-08-22 | P4 展现层完成：/api/notices 跨站视图折叠（同题同城→主行+N源，total=折叠组数）+ stage 阶段筛选 + /api/notices/{id} 详情（全字段+项目时间线）+ POST /api/notices/{id}/backfill 按需回填（ccgp=字段、其余=桥/ggzy 摘要+附件，如实落 detail_status）+ /api/notices/{id}/tenderfile 附件下载；台账页详情抽屉（CRM 操作区内改即时生效+时间线+回填按钮）+ 阶段标签/筛选 + 多源展开；CRM 计数折叠去重（招商商管 11→8、招商积余 6→3）+ 修 entity_suffixes 漏抽；CSV 导出加阶段列；实测：采招 HTTP 详情 405 WAF→改桥摘要路径、jsggzy/ggzy 列表无字段可复用（探明）、CHINABIDDING_COOKIE 未配置如实记录；测试 131 全量回归（新增 test_p4 11 例）；NAS 台账需重建容器生效 |
| 2026-08-23 | P5 岗位可信性+原发寻址一期：①修复测试覆写生产报告（run() 三路径 patch 隔离）②报告历史归档 reports/history/<runId>.json + 出勤简报 briefing.jsonl（失败站/待办/连续空窗 crawl/briefing.py）+ 任务书到期提醒（≤14 天）③原发探针实测：采招正文不披露来源（「来源」=资金来源误报）、ggzy b 页有结构化「信息来源：上海」+内容区项目编号/成交价格（回填 5090/5089 金额与编号）、jsggzy 有「来源：XX交易中心+URL」、ggzy「原文链接」JS 动态填充静态拿不到 ④crawl/origin.py（来源行解析抗噪+主体→平台映射，招商局 dzzb.ciesco.com.cn/华润守正 szecp.crc.com.cn 已核实）+ tenderfile ggzy 结构化字段/原发提取 + backfill 原发优先二次抓取（ccgp/ggzy/jsggzy 域）⑤notices 增 original_url/origin_source，详情抽屉展示原发链接；测试全量回归（新增 test_p5 11 例）；GitHub push 仍被网络阻塞（443 不通） |
| 2026-08-24 | 新增四站采集（分支 feature/new-sites-4 开发→已合 main，不动既有六站）：三级探路落位——**乙方宝 yfbzb=HTTP 一级直通**（搜索 table#treeTable+详情正文；m 域 403 走 www）；**千里马 qianlima=HTTP 一级直通**（POST /api/v1/website/search JSON 直调，详情 bid-<id>.html 419 反爬→详情走桥）；**工程帮(天工网) tgnet=Playwright 二级**（search.tgnet.com aspx JS 渲染取项目列表：阶段/时间/链接；bid.tgnet.com SSL 不通）；**瑞达恒(标慧帮) rccchina=注册墙三级不可直取**（手机号+短信验证码）——占位源如实报 register_wall+登记待办，待账号。注册表/sources.json/platforms.json/BROWSER_ROUTES/DETAIL_MODES 全接；tests/test_new_sites.py 11 用例；真机烟测 fetched=21（yfbzb 9+qianlima 2+tgnet 10）、rccchina 如实失败；全量回归 120 |
| 2026-08-24 | P6 管道接线与岗位分层（用户批准三步全做）：诊断「采集岗边界膨胀+下游对接缺失」——采集岗契约已冻结，台账/CRM/阶段/原发归平台设施层（.cursor/docs/pipeline.md）；采集员每轮自动落交接物 handoffs/collector/latest.json+归档（collector_run.py，items 与契约 output 同构）；总设计师工作区：parser 契约 v1.0.0→v1.1.0（tenderFile required→optional）+ MIGRATION 登记 + 管道契约文档补「四·五交接机制」节 + registry 修正（parser=current/v1.1.0/解析工作区）；parser_employee：implements 升 v1.1.0 + 管道入口 scripts/parser_from_handoff.py（筛有附件条目→逐条解析→写本岗交接物）；端到端验收：真实江苏公告(医院绿植租赁)+真实 .doc 附件 5460 字 → parser 真实 LLM 解析出完整需求矩阵（conf=0.8，缺失项如实 unresolved）；三区测试全绿（parser 18 用例）；提交：parser_employee 5a4f0a0、总设计师工作区非 git 仓库（文件直改）、collector 侧见本仓库提交 |
| 2026-08-24 | P7 召回自证+公司视角口径：实锤 ccgp 召回硬伤——连续 6 轮 raw=20 完全相同（max_pages=1 永远只扫第一页），「8城交集极少」是扫描策略假象；新增水位游标 crawl/watermark.py（每站原始已见 id 集合，出勤合并回报 wm_new/wm_total/pages，0 新增可自证）；ccgp 深扫+整页已见即停（SPIDER_CCGP_MAX_PAGES 默认 6）+ 时间段参数支持 + 列表代理解析；回溯作业 scripts/jobs/backfill_ccgp_window.py（逐日切片 62 天，断点续扫，实跑被 ccgp 频控阻塞如实记录待解封）；可投标线索口径 actionable=1（intent/bidding/change，实测 23/60）+ 台账默认勾选；简报增 coverage 节；修复旧用例页数假设；全量回归 159 全绿（新增 test_p7 5 例） |
| 2026-08-24 | P7 增补：出勤内建自动回填 auto_backfill_pass（可投标阶段缺金额的 ccgp/ggzy/jsggzy 新条目按条回填金额/编号/摘要，每站每轮 ≤5，失败如实记，SPIDER_NO_AUTO_BACKFILL=1 可关）+ 报告/简报 autoBackfill 节 + find_notice_id_by_item 回查；真实运行验证 ggzy 首轮 wm_new=20 水位落盘；test_p7 扩至 7 例；全量 162 全绿；ccgp 仍频控（两轮观察），回溯实跑待解封 |
| 2026-08-24 | P7 收口：深扫+已见早停覆盖全部 HTTP 列表源站——BaseSource 水位助手（_pages_cap/_page_all_seen/_count_page），ggzy/jsggzy/chinabidding/yfbzb/qianlima 默认 3 页、SPIDER_<SID>_MAX_PAGES 可调、整页已见即停；真实运行验证 ggzy 次轮 wm_new=0/pages=1 边界生效（0 新增自证闭环）；回溯作业断点续扫机制离线验证 2 例（失败日不推进/成功推进）；旧用例锁页修复；test_p7 扩至 11 例、全量 165 全绿；ccgp 仍频控（三轮观察），回溯实跑待解封 |
| 2026-08-24 | P7 ccgp 回溯闭环（桥路径）：HTTP 被封期间改 WebBridge 全窗口扫描——实测发现 ccgp 自定义时间参数服务端全不生效（表单/JS 实测 start_time/end_time 任意格式 + timeType 1~6），改 timeType=5 全窗口多页+客户端过滤；「绿植租摆」近半年总量仅 27 条（2 页）→ 全量扫尽后 8城×7-8月 kept=0（目标窗口内确无线索，可自证：水位 24+last_pages=6）；修冷启动边界缺陷（水位只来自第 1 页时整页已见不提前停，深度只增不减）+ 服务端重复页检测；crawl_ccgp_wb 支持多页/边界/水位合并/--start/--end；test_p7 扩至 13 例、全量 167 全绿 |
| 2026-08-25 | rccchina 手机号登录闭环基建：实测扩展 v1.11.6 工具清单（navigate/evaluate/network/fill/click/cdp/list_tabs 等 18 个）——CDP `Network.getCookies` 可用（含 HttpOnly）、`Network.getAllCookies` 挂起、`Browser.getVersion` 拒绝，工具清单入 webbridge_bridge.md；webbridge_client 增 cdp()/export_cookies()；captcha_flow.resolve_todo 三级导出（CDP 全量→document.cookie→warm 会话）；rccchina 源改为「无 cookie 挂登录待办（bid.rccchina.com/login）/有 cookie 如实报 cookie_ok_api_unmapped（搜索接口待登录态实测接线，不编 0）」；修 test_p4 分页断言过期（total≥items）；新增 test_webbridge_client 3 例 + rccchina/captcha CDP 用例，全量 172 绿（skip 3） |
| 2026-08-25 | qianlima 列表 418→桥路径：搜索 API search.qianlima.com/api/v1/website/search 直调 HTTP 418（站点级反爬，PowerShell/浏览器 UA 均 418 非请求头问题）；新增 scripts/crawl_qianlima_wb.py——真浏览器打开搜索页→页内 fetch POST 同款 API→复用 QianlimaSource._parse（契约一致）；BROWSER_ROUTES/platforms.json qianlima route=http→webbridge；anti_bot.json/sources.json/AGENTS.md/当前阶段.md 口径同步；每词间隔防连发、部分结果保留（SourceError.partial 语义→status=partial）、错误如实记 crawl_runs.note；详情维持 bridge 可空；tests/test_new_sites.py 补 qianlima 桥模块解析/路由/URL 等价/JS 构造 4 例，路由断言改 webbridge；实网最小探针（2 次，桥在线 ext=1）：页内 fetch 与顶层导航仍 HTTP 418（CloudWAF 硬拦截，事件ID 30-clou-73-20260825024133-16634215，无 JS challenge 自动解）→ 站点级 IP/设备风控封禁未解，恢复产出待解封（诚实记 crawl_runs.note，不编 0） |
| 2026-08-25 | qianlima 418 二次探针（真人 UI 流，scripts/probe_qianlima_ui.py → data/qianlima_ui_probe.json）：官网首页正常、WAF cookie（HWWAFSESID/WAFSESSCC_TAG 等）正常下发、搜索页壳正常渲染，但站点自己前端发的全部 /api/v1/* 请求（含其自带 3 次 timeType 降级重试）同样 418 → 确证 IP/设备级硬拦截，真人操作同样无结果，非模拟不足；随之加固 crawl_qianlima_wb：遇 418 只探 1 词立即停手（WAF_BLOCK_MARKERS + 停词逻辑，频控靠冷却不靠轰炸，下轮自愈探测），tests/test_new_sites.py 补 waf_418_stops_early 例，全量 177 绿 |
| 2026-08-25 | qianlima 418 解封验证 + 代理能力落地：用户开启 HK 出口代理（Ikuuu，出口 103.151.172.39，系统代理 127.0.0.1:63009 全机生效）→ 冒烟 1 词 items=20（API 200）→ 20 词全量 raw=345 kept=62 upsert≈73（status=partial：2 词桥 evaluate 偶发超时，其余 16 词均有入库）；新增代理配置层 config/proxy.json（per_source > default > env SPIDER_PROXY，仅 http/https，非法 scheme 响亮报错）+ config_loader.proxy_for + HttpSession ProxyHandler 注入 + 千里马配置代理时退回 HTTP 内核路径（collector_employee._run_platform）+ scripts/probe_qianlima_proxy.py 验证脚本；tests/test_proxy.py 8 例（优先级/非法 scheme/Handler 注入/路由切换），全量 185 绿；发现 urllib 默认 ProxyHandler 即走系统代理——TUN/系统代理模式下所有 HTTP 源自动换出口，无需改配置 |
| 2026-08-25 | 瑞达恒「邮件验证码回传」登录闭环（人不在家也能登录）：scripts/email_gateway.py（Gmail SMTP 发信 + IMAP 收信 + 验证码提取；Python 栈不走系统代理 → 实现 HTTP CONNECT 隧道，代理取 EMAIL_PROXY/SPIDER_PROXY/系统代理；收信只按 #tag 主题过滤，尝试后标已读防重放）+ scripts/jobs/rccchina_email_auth.py（request→轮询→complete 一条龙：桥填手机号点获取验证码→发请求邮件→IMAP 收回复提码→桥填码登录→CDP 导出 Cookie 落盘→关待办→回确认邮件；marker≤6h 防重复发信，8 分钟超时本轮放弃）+ 采集员 hook（rccchina 撞墙 run() 末尾自动跑闭环，SPIDER_NO_EMAIL_AUTH=1 可关；report 增 emailAuth 字段）+ .env.example 增 GMAIL/RCCCHINA_PHONE/EMAIL_TO/EMAIL_PROXY 说明 + 文档 .cursor/docs/rccchina_email_auth.md；tests/test_email_auth.py 16 例（验证码提取/发信/隧道握手/收信过滤/闭环状态机），全量 201 绿；实测发现本机直连 imap.gmail.com 超时（国内网络），经代理隧道可通待凭据实网验证 |
| 2026-08-25 | 邮件网关实网凭据测试与双通道落地：用户提供 Gmail 应用密码（发信 pmlx…/收信 xnya…）→ 实测矩阵：Gmail IMAP 993 直连登录成功（应用密码有效、收件箱 2967 封）；Gmail SMTP 465/587/25 直连全部 TCP 超时（本网络封锁发信端口）；走 HK 代理 Gmail 被 Google RST（数据中心出口黑名单）；无 IPv6；对照 QQ/Outlook 465/993 走隧道 TLS 正常 → 网关加 provider 抽象（EMAIL_PROVIDER=qq|gmail，qq: smtp.qq.com/imap.qq.com + QQ 授权码；gmail: 原配置）+ imap_status() 健康检查；结论：发信走 QQ 通道、收信 Gmail/QQ 双可用，待 QQ 授权码后闭环可跑 |
| 2026-08-25 | 瑞达恒暂缓（用户决定）+ 千里马确认主分支：qianlima 全部代码已合 main（9d8f52e/8e54493/e69b2cc 均位于 main，feature/new-sites-4 已完全合并）；rccchina 在 platforms.json/sources.json 置 enabled=false（注册墙暂无账号），邮件验证码闭环代码保留待启用；口径改「九站运行 + 瑞达恒暂缓」，AGENTS.md/当前阶段.md/Windows 任务描述同步 |
| 2026-08-26 | 补发布日期 + 台账默认发布时间倒序（用户指令）：①jsggzy 省站搜索 API 实际字段探明——infodatepx/infodateformat=详情页「信息发布时间」（6/6 实测一致），webdate/publishTime 不存在（历史 20 条因此 publish_date 全空），linkurl 8 位日期段与 infodate 是入库/迁移日不可用（如 URL 20200921、真实 2020-05-07）；源解析改 infodatepx→infodateformat→webdate→publishTime 链 + live 测试断言；②新增 scripts/backfill_publish_dates.py（详情页实证「信息发布时间」+标题核对，dry-run 默认）补录存量 20 条（2020 年 13 条/2021 年 5 条/2024 年 2 条），报告 data/backfill_publish_report.json，证据不足保持 NULL 不造假；③台账排序默认「按发布时间（新→旧）」（ledger_app.html 选中 + api.py/ledger_data.py 默认 sort=publish），空日期排最后；④新建 .cursor/docs/sources/jsggzy.md；全量 202 测试绿（skip 3） |
| 2026-08-26 | 台账升级到 NAS（用户指令）：新增 scripts/nas_upgrade_ledger.py——SSH+scp -t 上传（Synology 未开 sftp 子系统）代码树 crawl/scripts/config/sql + 壳页 data/web，保留远端 .env 与 docker-compose.yml（CRAWL_CRON_HOURS=off 展示口径不动），sudo docker-compose up -d --build 重建（198 文件，pip 层缓存命中 32s）；自检通过：容器 healthy、壳页含新排序默认项、/api/notices 默认发布时间倒序（首条 2026-08-25 15:05:06）；NAS 8765 展示与仓库 HEAD 对齐 |
| 2026-08-26 | 任务完成自动发「增量简报」邮件（用户需求）：新增 crawl/mail_report.py（HTML 简报：本轮新增数/城市分布/关键词分布/新增明细表/失败·空平台警示，email-safe 全 inline 样式，明细最多 80 条）+ email_gateway 增 send_html/send_mime（multipart/alternative）+ 发信回退（代理隧道失败→直连）；采集员 run() 追踪本轮真正新增 newNotices（去重后），collector_run.py 与 run_incremental.py 完成钩子自动发信（SPIDER_NO_EMAIL=1 可关，失败不挡主流程）；tests/test_mail_report.py 11 例 + email_auth/collector_contract/p5/pipeline 相关套件回归绿；实测本机直连 smtp.gmail.com:465 发信成功（与 08-25「Gmail SMTP 直连超时」结论相反——网络已变，直连当前可用），收件 EMAIL_TO=279152260@qq.com |
| 2026-09-18 | 新增「深圳阳光采购平台」szexgrp（用户指定）+ 桥自愈重大修复（用户主线：爬虫不该因桥断而停）：**①新源 szexgrp**——`POST https://ygcg.szexgrp.com/api/v1/trade/content/page` JSON 直调，**无登录/无验证码/无 WAF**；`fields=null` 一次拿全类型、`releaseTimeBegin/End` 服务端时间窗（实测 1149→75 条/30 天）、city 固定=深圳（8 城之一）；详情页是 JS 壳，正文走 `/api/v1/rhgw/szjy/detail?sectionCode=`；**类型白名单**只收公开采购全周期（采购公告/变更公示/候选人公示/结果公示/定标结果公示），**邀请函(邀请投标)/合同续期公示/其他公示 丢掉**（真机 60→27 条、邀请函=0）；接入 REGISTRY/SOURCE_ORDER/sources.json/platforms.json/SUMMARY_SOURCES/DETAIL_MODES(szexgrp_http)+tests 5 例；口径九站→十站。**②桥根因修复**——旧 `scripts/webbridge_server.py`（SpidermanWebBridge 任务动作）占着 10086 且无 `/status` → **官方 daemon 起不来、扩展连不上 → 所有 WebBridge 源静默 0 条**（江苏站空跑 2839s/47 分钟为实证；旧桥自报 extensions_connected=0）；处置=停旧桥 → 起官方 daemon（v2.0.5→**v2.0.15** upgrade）→ 扩展连上（`extension_connected=true`，navigate 实测 True）→ 任务动作改 `scripts/wb_bridge.py watch`。**③可用性严格判定**：`wb.available()` = daemon 在线 **且** extension_connected（旧实现只看端口、连 HTTPError 都算 True → 扩展掉线误报「可用」）。**④桥自愈三层**：常驻 `wb_bridge.py watch`（120s 巡检 + 心跳 `data/web/wb_watch_heartbeat.json` + 单例 + 单次异常不杀）+ `ensure-daemon`（心跳陈旧后台 detached 重拉，采集员每次跑会调）+ 采集时 `_wait_bridge_ready`（掉线等恢复默认 300s、同一轮 gave_up 后跳过其余 webbridge 源、HTTP 源不受影响）；不断依赖改任务触发器（`Set-ScheduledTask -Trigger`/`Register-ScheduledTask` 实测 Access denied 需提权）。**⑤桥脚本 fail-fast**：江苏站（导航失败立即抛错 + 连续 3 词无结果提前停）、千里马（`_is_bridge_down` 与 418 一样立即停）。**真机故障恢复实测：杀 daemon → 保活 106s 自愈**。测试新增 tests/test_szexgrp.py(5)/test_bridge_resilience.py(6)/test_wb_bridge_keepalive.py(7) + test_collector_contract 增 BridgeAvailable(3)/EnsureBridge 对齐 `_start_official_daemon`，全量 83+ 全绿 |
| 2026-09-18 | 浏览器口径收口「**统一走 Chrome，绝不走 Edge**」（用户要求复核，查出确有漏点）：①删死代码 `EDGE_CANDIDATES`（webbridge_client 里定了 Edge 路径但从未使用）；②新增 `webbridge_client.open_in_chrome(url)`——只用 `CHROME_CANDIDATES` 打开（Chrome 已在跑则开新标签、否则起 Chrome；找不到 chrome.exe 返回 None）；③**修真实漏点**：`crawl/captcha_flow.py` 两处兜底用 `webbrowser.open()`（走**系统默认浏览器**，默认是 Edge 就落到 Edge）→ 改 `open_in_chrome()` 并移除 `import webbrowser`；④`scripts/webbridge_server.py`（已弃用）顶加醒目弃用横幅 + 残留 Chrome/Edge 文案改 Chrome；⑤测试：test_bridge_resilience 增 `TestOpenInChrome`(3 例：用 chrome.exe / 无 Chrome 返回 None / 候选路径全 chrome 且 EDGE_CANDIDATES 已删) + test_all 验证码用例改 mock `open_in_chrome` + `_wait_bridge_ready` 用例 mock `wb_bridge.ensure_daemon`（隔离真实拉保活副作用）；AGENTS.md/当前阶段.md/webbridge_bridge.md 口径补「统一走 Chrome」。回归全绿 |
| 2026-09-19 | 采集员入口「启动自记录」+ 09-18 22:00 出勤失败取证（用户要求核对本地代码/任务是否最新 + 带最新代码跑一轮）：**①22:00 那轮的取证结论**——Task `SpidermanCollector` 22:00:01 启动、`LastTaskResult=1`，但 `crawl_runs` **零行**（11:00 那轮最后一行 12:38:31，下一行直接是 09-19 00:02:43 手动起的）、`reports/` 与 `handoffs/` 全停在 12:40:10、`briefing.jsonl` 无追加。逐项排除：无重启（LastBootUpTime=09-18 18:42:01）、无关机/异常事件、应用程序日志无 python 崩溃、工作区 18:32–23:21 **无任何文件改动**（⇒ 22:00 时 = 已提交的 `158dabd`，**不是我改代码的中间态**）、PATH 上的 python 就是任务用的 Python310（3.10.11，导入链 exit 0）。而 11:00 那轮 ccgp 的 `crawl_runs` 行出现在 **11:00:05**（启动后 5 秒）⇒ **零行 ⟺ 进程在启动约 5 秒内即死亡**（外部终止或启动期异常）。**②真正缺口 = 完全没有日志**：任务动作是裸 `python.exe scripts\collector_run.py`，Task Scheduler 不落 stdout，且本机 `Microsoft-Windows-TaskScheduler/Operational` 处于 **IsEnabled=False**（无任务历史）⇒ 启动期死亡无痕、事后不可定位（这也是本次无法给出唯一根因的原因，如实记录）。**③修复 = 入口自记录**：`scripts/collector_run.py` 在**任何重导入之前**装 tee（stdout/stderr 全量 → `logs/collector_run_<时间戳>.log`，只保留最近 30 份）+ 写启动戳 `logs/collector_launch.json`（pid/时间/cwd/argv/解释器/日志名）+ `faulthandler.enable`（段错误一类硬崩也留栈）+ `__main__` 兜底 `BaseException`（打栈 + 明确 exit 1）；**被 import 时零副作用**（`tests/test_pipeline.py` 按路径加载该模块，故日志安装放在 `if __name__ == "__main__"` 内且必须早于重导入）；`logs/` 入 .gitignore；新增 `tests/test_collector_run_logging.py` 5 例（日志+启动戳落地 / `SPIDER_NO_RUN_LOG=1` 可关 / import 无副作用且不抢 stdout / 只保留最近 30 份 / **未捕获异常必留栈且 exit 1** —— 用伪造 `ce.run` + 非法 HANDOFF_DIR 真跑子进程，不碰外网与邮件）。**④带最新代码跑一轮实测**：确认工作区 = `main`(`1b2f5d9`)、任务 WorkingDirectory = 工作区 ⇒ 任务跑的就是最新代码；09-19 00:02:40 起跑十站完整一轮（含 szexgrp）核对产出。**⑤全量回归并修两处 09-18 遗留红测**（跑起来才暴露，`158dabd` 当时只跑了部分套件）：`test_proxy.test_no_proxy_handler_when_direct` 断言的 `len(ProxyHandler)==1` 是**错的实现细节**——实证 `build_opener(ProxyHandler({}))` 因空 dict 不生成 `http_open`/`https_open`、只剩被 `add_handler` 跳过的 `proxy_open` ⇒ `added=False` ⇒ 该 handler 根本不进 `opener.handlers`（带非空代理时才会进）；**直连行为本身是对的**（无 proxy handler = 不走任何代理），改断言「有效代理映射为空」并把 `urllib.request.getproxies` 伪造成有值 ⇒ 一旦有人删掉 `http_session` 的显式直连分支，默认 ProxyHandler 会读系统代理而必红（已实证：回归→`{'http': 'http://127.0.0.1:7890'}`、现状→`{}`）；`test_new_sites.test_waf_418_stops_early` 是 `158dabd` 改写千里马停手文案时把 `waf_block` 标签删掉了，改为按原因分流输出 `waf_block` / `bridge_down`（错误串自证）。**全量 301 项 OK（skip 3）** |
| 2026-09-19 | 00:39 采集进程被外部强杀事故复盘 + 三处「死亡 = 数据/证据丢失」修复（用户报「739 jiangsu_zhaobiao running 还是卡死」）：**①事实**——00:02:40 起跑的十站轮次，ccgp 30 / chinabidding 95 / ggzy 74 / jsggzy 66 / cebpub 69 全部 success；`jiangsu_zhaobiao`（行 739，00:31:14 起）**桥已就绪且真出数**（`[collector] webbridge ready for jiangsu_zhaobiao` + 29 个词的真实条数 18/10/5/24/18/2/21/7/14/14/…），到第 30 个词 `球场养护` 时入口日志停在 00:39:08；同一分钟内**保活 watch（心跳停在 00:38:26）与 kimi-webbridge daemon 一起消失**。排除项：无重启（`LastBootUpTime` 仍 09-18 18:42:01）、无睡眠/关机事件（System 42/107/6006/6008 全无）、00:35–00:50 System 与 Application **一条事件都没有**、项目代码里**没有任何批量杀进程逻辑**（全仓只有 `wb_bridge.py stop` 按 pidfile 杀一个，无人调用）⇒ 与 09-18 22:00 同属「外部/会话层静默全灭」，**根因仍不可唯一确定**（如实记录；但这次顺带挖出三处真缺陷并修掉）。**②数据全丢**：`crawl_jiangsu_wb.py` 把 `upsert_notices` 放在整个词循环**之后** ⇒ 29 个词的结果一条没入库（notices 里江苏当天更新 0 条、最后更新仍停在 09-12），直接违反项目铁律「任何源站失败都不得丢弃已采部分结果」。修复=**逐词增量落库**（jiangsu + qianlima 同样处理），结尾保留幂等兜底；异常退出如实报 `partial` + 真实条数 + 已采 notices（绝不报 0 / 假 0）。**③台账永久「卡死」**：进程被杀时 `finish_run` 永不执行，行永久停在 running（`start_run` 只在同源下次再跑时顺带清扫，被杀的若是最后一个源要等下一整轮十几小时）。修复=新增 `crawl.db_store.sweep_orphan_runs(max_age_hours=3)`，每轮采集开头全局闭环并写入报告 `orphanRuns`；实测把行 739 闭环。**④保活自己是黑盒**：`wb_bridge.py watch` 的 stdout/stderr 被 `ensure_daemon` 指向 DEVNULL ⇒ 它是怎么死的同样查不到（00:38 那次的直接后果）。修复=新增共用 `crawl/run_log.py`（Tee / open_log / install / prune）+ 保活自记录 `logs/wb_watch.log`（>2MB 轮转），watch 在**任何重导入之前**装日志；采集员入口改用同一实现（行为与测试不变）。实测重启保活后日志立即记下一次真实故障与自愈：`before {"up":false,"extensions":0} → ensure {"bridge":true,"extensions":1}`。测试新增 `tests/test_run_durability.py` 15 例（逐词增量 / 中断报 partial / 兜底失败不丢数 / 孤儿清扫 SQL 与参数 / watch 自记录与 import 零副作用 / run_log 原语）。 |
| 2026-09-19 | 打通详情/正文链（用户选定优先级 A；起因：体检发现「只剩千里马」不成立）：**①体检结论（数据）**——千里马近 7 天 13 轮 0 success/2 partial/11 failed，但**11 次失败多是桥掉线**（`bridge_evaluate_failed: 浏览器扩展未连接`，09-16/17/18，即 09-18 已修的桥根因），桥修好后 09-19 08:47 即 `partial` 出 122 条、入库 103 条 ⇒ **千里马确实只剩 WAF/418 的 IP 标记**，且近 7 天仍贡献 107 条（第三高）。真正更严重的是另两件：**详情/正文链几乎没产出**（`detail_status` 3103/3166 为 null、`tenderfile_path` 仅 7 条、本轮 `detail_fetch_success_rate=0.1`）与 **tgnet 每轮白刷 373 条历史项目**（publish_date 跨 2008–2026，近 30 天 16 条、近 7 天 0 条）。**②根因不是反爬而是「没接线」**：完整链（详情→原发寻址→原发转爬 + AI 兜底）唯一入口 `scripts/collector_detail.py` **没有接进任何定时任务**（本机无权新建计划任务，`Register-ScheduledTask` 需提权）；轮内 `enrich_source_details` 只做**字段级**且以 `amount IS NULL` 为条件、`auto_backfill_pass` 只碰 ccgp/ggzy/jsggzy 且仅限「可投标阶段 + 缺金额」。**③三处修复**：新增 `crawl/detail_pass.py`（共用编排：可投标阶段优先 + 发布时间新→旧；单轮总数/每站条数/总耗时三重封顶；瞬时失败 `err:bridge/detail_page/fetch` 可重试、永久失败（验证码门/登录墙/无附件）不重打；正文计数 `text_ok` 如实）→ **接进 `collector_employee.run()`**（配置 `config/anti_bot.json:detail_pass`，默认 80/10/900s，`SPIDER_NO_DETAIL_PASS=1` 关、`SPIDER_DETAIL_AI=1` 开 AI，cebpub/rccchina 默认排除），`scripts/collector_detail.py` 改为同一实现的「手动不限时」入口；**`SUMMARY_CHARS` 200 → 2000**（实测 jiangsu/yfbzb/chinabidding/qianlima/tgnet 摘要均长与最长**正好 200**、全被截，而 ccgp/szexgrp 早就是 2000 —— 口径本就不一致，200 字等于没正文）；**ccgp 字段分支不再跳过附件链**（原实现在字段抓完就 `return`，ccgp 一条正文都没落过）。**④实测**（12 条 / 143s）：summary 从 200 → **1841~2000 字**、全库 ≥1000 字的 0→26、附件正文 8→11（jiangsu 3 条 PDF/DOCX 真落盘）。新增 `tests/test_detail_pass.py` 14 例；`test_collector_contract`/`test_p5` 用 `SPIDER_NO_DETAIL_PASS=1` 保持离线（新阶段会查真实 DB 并访问外网）。 |
| 2026-09-19 | 详情链生产验证 + 候选池均衡修复（11:00 定时轮实测）：**①生产实测**（`SpidermanCollector` 11:00 轮，Task `Last Result=0`，总耗时 64min）——报告 `detailPass`：候选 320、**处理 67 条**、`detail_ok` 62、**拿到附件正文 8 条**、摘要 58 条、失败 5、耗时 455s；库内 `detail_status` 为空 **3103→2998**、summary ≥1000 字 **26→80**、附件正文 **11→19**（jiangsu 15 / jsggzy 3 / ggzy 1）。**②暴露并修复一个缺陷**：`processed=67 < 上限 80` 却报 `candidates_exhausted` —— 候选池是**全局排序**的，前几个站把「每站上限 10」吃满后池子里剩下的全是这些站、没有可用槽位，白扔 13 个配额。修复=窗口函数**按站均衡取候选**（`ROW_NUMBER() OVER (PARTITION BY source_id ORDER BY <阶段优先级>, publish_date DESC, id DESC)` 后取 `rn <= 每站上限`，MySQL 8.0.28 实测可用）⇒ 池子从「前几站吃满」变为 9 站各 10 条 = 90。**③如实记录两项未变/未解**：契约指标 `detail_fetch_success_rate` 仍是 0.1 —— 它量的是**本轮契约 items 的详情抓取率**，与新阶段测的「历史回填覆盖」不是一回事（契约 7 项指标冻结，故不混口径，新阶段另出 `detailPass`）；本轮 `platform_success_rate.qianlima=0.0` 且进 `empty_platforms`（418/WAF 又被标记，属已知自愈探测）；送信钩子失败 `[mail] ok=false [WinError 10060]`（SMTP 连不上，如实记、不挡主流程）。**④仍待用户拍板**：tgnet 每轮白刷 373 条历史项目（近 7 天 0 条进窗口）该如何处置（收窗/降权/停用）—— 本次只做了用户选定的 A。 |
| 2026-09-19 | WebBridge 标签卫生（用户报「Chrome 里 tab 越堆越多、吃内存」）+ tgnet 收窗：**①问题实证**——详情抓取为每条公告开一个桥会话 `tf-<站>-<md5(url)[:8]>`，而 `list_tabs` **按会话隔离**（别的 session 看不到本会话的 tab；我最初查默认 session 得到「0 个」是假象）。原实现只在 `collector._enrich_tenderfiles` 结束时 `close_bridge_tabs()` 关一次，**新增的详情补全阶段根本没调它** ⇒ 22:00 轮日志里只有 jiangsu 自己那句 `closed 1 tabs`，详情轮 80 条留下的桥会话（chinabidding/jiangsu/qianlima 各 10 个）从未释放，峰值约 40 个 tab/轮（现存 0 个遗留，应是用户手工关掉的）。**②修复四处**：(a) `webbridge_client.close_session()` —— 用扩展自带 `close_session` 一次收干净（实测 `{"success": true, "closed": 2}`），老版本扩展退回逐个 `close_tab`；(b) `crawl/tenderfile.py` 的会话登记**落盘** `data/web/wb_open_sessions.json`（原先只记内存，进程被强杀后那批 tab 就永远没人关），新增 `bridge_session_name / release_session / close_bridge_tabs(stale_only_min=)`；(c) `fetch_detail_via_bridge` / `fetch_cebpub_via_bridge` 改为「瘦包装 + finally 释放自己的会话」——单条抓完即关，峰值从「一轮几十个」降到 1；实测单条 chinabidding 详情 16.7s，抓后该会话 tab=0、登记文件空；(d) `scripts/wb_bridge.py` 新增 `clean-tabs [--stale-min N]`，且常驻 watch **每 10 轮（约 20 分钟）自动清扫登记超过 90 分钟的陈旧会话**（只读登记文件 + 调桥，保活进程保持轻量）。**③tgnet 收窗**（用户授权按我判断处置）——实测 tgnet 是**唯一**违反过滤口径的源：385 条里 **340 条早于窗口起点、319 条不在 8 城**（跨 2008–2026 的工程库），每轮还把 373 条重刷一遍；根因 `crawl_tgnet_pw.py` **直接 upsert、没有任何城市/日期过滤**（jiangsu/qianlima 各自实现了，其余源均 0/0 越界）。修复=抽出共用 `crawl/source_filter.py::filter_notices`（口径与 HTTP 内核 runner 完全一致：8 城白名单 + 发布时间窗、无日期不丢），qianlima 改为委托、tgnet 入库前过滤，note 记 `raw/kept/city_date_drop`；**决定：保留 tgnet 启用、只收窗，不降权不停用**。**④如实记录**：存量 340 条越窗 tgnet 行**未删除**（删数据属破坏性操作，登记待办：一行 SQL 可清）；送信钩子该轮 `[mail] ok=false [WinError 10060]`。测试新增 `tests/test_wb_tab_hygiene.py` 13 例（close_session 双路径 / 登记落盘与释放 / 陈旧阈值 / 异常路径也释放 / watch 清扫 / 内核同口径过滤）。 |
| 2026-09-20 | 新岗位「源头追溯员 / tracer」（用户提出：采集入库后没人管「这条公告的源头在哪」）：**①为什么独立**——采集员按站×关键词保广度、失败绝不阻塞；追溯员按 `project_key` 一对一保源头、允许 `not_found/portal_down` 终态并要求人工接手，两者粒度/预算/失败语义全不同。**②三层内核**——`crawl/origin_hints.py`（规则+AI 锚点：采购人/邮箱域名/正文URL/项目编号/发布媒介行 + 兄弟公告补锚点 + 四类检索词）、`crawl/origin_portals.py`（`config/origin_portals.json` 声明式适配器 + 桥驱动 + Bing 并集检索）、`crawl/origin_trace.py`（选种子→汇锚点→找源头→校验→取回→落库）。**③四道防假阳性校验**（实测踩出来的）：企业信息站域名否决（qcc 曾被误认源头）、镜像页否决（品牌名/会员门）、列表页否决（公告类词堆量，拦下 m.bidnews.cn 公司专栏 22 次）、项目核心名/编号硬锚点 + 日期 ±45 天（拦下 ccgp 上同一学校 2026-07 的另一次采购）；标题相似度改用 Dice 系数（覆盖率会把「变更公告」与「原公告」都判 1.0 而抓错详情 id）。**④落库口径**：`notices` 新增 `origin_level/origin_portal/origin_detail_url/origin_status/origin_confidence/origin_evidence/origin_traced_at`（`scripts/migrate_origin_trace.py` 幂等迁移），与采集员 `detail_status` 完全隔离；正文仅在严格更好时覆写，拿到原始采购文件则无条件覆写；同组结果广播全 notice。**⑤实测**（室内绿植类）：华能 50343 → `ec.chng.com.cn` detail id=12930997 一手正文 1761 字（conf 0.98，method=portal_registry）；杭州创悦盈 → 招必得签章版采购文件 PDF 184KB 落盘（含最高限价 19.6406 万）；相城实验小学 → 如实 `not_found` + 候选与否决原因；东部机场自建平台 502 → `portal_down` 如实登记。**⑥契约与测试**：`contracts/tracer/v1.0.0.json` + registry 登记（current）；`tests/test_origin_trace.py` 33 例（纯函数 + 注入式假 IO，不碰外网/真库）全绿；文档 `.cursor/docs/origin_trace.md`。**已知限制（如实）**：搜索引擎未收录的一手平台（招必得）自动发现会失败，靠人在环 `--origin-url` 沉淀。 |
