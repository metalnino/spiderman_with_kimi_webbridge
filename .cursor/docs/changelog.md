# 过程决策简记

| 日期 | 决策 |
|------|------|
| 2026-08-09 | 四站固定；江苏搁置；MySQL utf8mb4；HTTP主 WebBridge辅 |
| 2026-08-09 | 建立 `.cursor/docs` 为过程文档唯一源；验收看阶段勾选 |
| 2026-08-09 | 仓库：https://github.com/metalnino/spiderman_with_kimi_webbridge |
| 2026-08-09 | P0 落地：crawl 包四站增量入库 + incremental.html；T1–T5 自测通过（库约 73 条） |
| 2026-08-09 | P1/P2：清洗/级联看板/词库/监控/AI钩子/人工标记/验证码待办；unittest 14/14 |
| 2026-08-09 | P3：jsggzy（全国 ggzy 江苏切片）+ 采招 Cookie 详情可选 + CRM/entities；unittest 18/18 |
| 2026-08-09 | 台账改为本机只读 API（127.0.0.1:8765）+ ledger_app 壳页；静态 dashboard 作离线快照 |
| 2026-08-09 | 验证码：本机同事划（打开/已解决）+ Cookie 落盘回灌；台账仅放行 captcha POST |
| 2026-08-09 | NAS Docker：爬虫+台账容器（MySQL 沿用）；compose 暴露 8765 |
| 2026-08-11 | 接入江苏招标网 jiangsu_zhaobiao（账号登录可选+滑块人工；列表 Dqsearch） |
| 2026-08-14 | 修复主入口 ImportError（build_incremental_html→list）；网络测试 SPIDER_LIVE_TESTS=1 门控；http 4xx/5xx/网络错误分类；关键词上限可配(默认不限)；验证码待办去重；6 站配置/看板同步；补根 README；unittest 26/26 |
| 2026-08-14 | 详情字段抓取起步：crawl/detail.py 新增 ccgp 详情 HTTP 解析（金额/招标人/代理/项目编号）并接入 runner 回填；探针确认 ggzy=JS壳、jiangsu=Cloudflare、cebpub=验证码、chinabidding=登录墙（待接入）；unittest 27/27 |
| 2026-08-16 | 两路合并并 NAS 部署：接入 jiangsu HTTP 521 的 __jsl_clearance_s 纯 HTTP 解；定时改固定时刻 8/12/18/22 东八区；CRM 主体规范化全国合并(uk_name)；SSH 重建镜像验证 healthy |
| 2026-08-16 | 台账后端换 FastAPI（api.py + uvicorn，契约不变/自动 /docs）；源站精简为 4 站（停用 ggzy/jsggzy，代码保留）；Dockerfile 用清华 PyPI 镜像 |
| 2026-08-17 | 本机并行重跑(绿植租摆/8城/7-8月)：ccgp 可用但 8城+7-8月交集极少；加发布时间范围过滤；cebpub Playwright 渲染+提取已通、关键词过滤未通(加密搜索 searchName 未传)；chinabidding info_search 超时；jiangsu 521(JSL 未解)。以上为待修项 |
| 2026-08-18 | 六站全部攻克定稿：jiangsu WebBridge 真浏览器(JSL 两阶段) 11 条入库；cebpub Playwright performSearchRequest 5 条入库（见 anti_bot_lessons.html）；NAS 此后弃用，本机为唯一运行点 |
| 2026-08-19 | 员工化套壳：招标采集员 collector/v1.0.0 外壳四层 + 契约测试；配置层 keywords/platforms/filters.json；观测报告 reports/collector-report.json；内核仅 2 处增量 seam |
| 2026-08-20 | 六站全开（长期口径）：platforms.json 全 enabled + 路由(ccgp/chinabidding/ggzy/jsggzy=HTTP，cebpub=Playwright，jiangsu=WebBridge)；内置桥服务端 scripts/webbridge_server.py（标准库，Chrome 扩展自动连）；scheduler 支持 CRAWL_MODE=collector；本机定时任务 SpidermanCollector(8/12/18/22)+SpidermanWebBridge(登录启动) |
| 2026-08-20 | 采集员契约对齐 collector/v1.2.0：publishTime 空串→null；新内核 crawl/tenderfile.py（详情抓取+附件下载+PDF/DOCX/TXT 正文清洗+summary）；output 新增 tenderFile（可空）；观测第 7 指标 detail_fetch_success_rate；测试 47 契约用例 + 91 全量回归；ccgp 附件真下载真提取（415KB doc→47k 字）；registry collector → current |
| 2026-08-20 | tenderFile 详情抓取全平台路由（v3.1）：ggzy/jsggzy 正文 b 页 HTTP SSR 直取（a→b 纯函数转换）；chinabidding(405 WAF)/jiangsu(521) 走 WebBridge 真浏览器详情（摘要可达，附件登录墙如实 null）；cebpub vaptcha 人工验证码阻塞如实跳过；每平台/全局限额护栏；实测 3 站详情摘要 6/6 填充；测试 53 契约 + 97 全量回归 |
| 2026-08-20 | 江苏账号登录+会员附件真下载（v3.2）：桥内自动登录（tianai 滑块人工一次、会话长期复用）；导出登录 Cookie 走纯 HTTP 下 bidFiledown.jsp 附件（魔数嗅探判型：pdf/docx/旧版 .doc，HTML 拒绝）；旧版 WPS .doc 正文提取（分段表解析+字节打捞 utf-16le）；实测 .doc 5460 字、.pdf 1256 字全提取；测试 55 契约 + 99 全量回归 |
| 2026-08-22 | 江苏登录数字验证码方案落地并搁置（v3.3）：定位会员登录走 /common/img.jsp 数字验证码（60x20 粘连图）；实现 HTTP 验证码登录全流程（取图→OCR→loginPost(loginType=0)→会员中心 homePageUc.do 真实验证态→Cookie 落盘，限 3 次）；OCR 双后端 crawl/captcha_ocr.py：DeepSeek 多模态 deepseek-v4-flash-vision-exp（OpenAI 兼容，配置位就绪待 key）+ 确定性识别（渲染数字回读自测过）；确定性识别对真站图实测连续错误→按指示搁置，OCR 未配置时跳过 HTTP 尝试不空耗，待办已登记；测试 61 契约 + 105 全量回归 |
| 2026-08-22 | DeepSeek 多模态验证码识别启用、登录打通（v3.4）：数字员工专用 DeepSeek 官方 key 明文配置 config/ocr_api.json（gitignored）；学官方 vision 文档修正（思考模型 max_tokens 2000、content 空回退 reasoning）；识别增强（三视图预处理 6x/二值/反相 + 双读一致性投票）；修正 loginType=userId（原 "0" 被服务端静默拒绝）、跟随 SSO loginSuccess 回跳、同图大小写变体重试；真机多次登录成功（会员中心验证通过、Cookie 落盘、待办关闭）；遗留：SSO 跨子域传播最后一环未通（江苏站静态页仍未识别登录态，桥内真实登录已覆盖采集）；测试 62 契约 + 106 全量回归 |
| 2026-08-22 | cebpub 详情能力开发（v3.5a）：反出 SPA app.js 加密——DES-ECB-PKCS7 key=1qaz@wsx3e(前8字节)，实现 cebpub_des_decrypt；附件接口 getBulletinAttachmentUrl?bulletinId&interfaceSource=0 已调通（解密后 JSON）；getBulletin 403 解密=「未登录」；DETAIL_MODES cebpub→bridge_vaptcha：桥内尝试、vaptcha 未过如实报并登记待办、人工一次后附件自动可下；测试 64 契约 + 108 全量回归 |
| 2026-08-22 | 完成度审计收口（v3.5）：修 cebpub Playwright 列表缺 detail_url（showDetails 末段 32hex=uuid，真机验证）；江苏桥会话过期自动重连——桥内 AI OCR 登录（页内 IIFE fetch 验证码→DeepSeek 识别→#zh 切账号 tab→IIFE 提交，真机登录成功跳用户中心）；修桥 evaluate 语义（async 函数体不执行→改 IIFE Promise）；OCR 双读不一致信首读（靠服务端反馈闭环）；六站全链路收口，测试 65 契约 + 109 全量回归 |
| 2026-08-22 | P4 展现层对齐开工：需求对齐（详情抽屉/跨站视图折叠/回填分级）落 phases/P4.md；新增**公告阶段识别与项目时间线**维度（用户补充需求：招采全流程数据都要保留并按时段分割，为后续做标书提供历史结果）——crawl/stage.py 9 阶段规则（标题关键词+源站编码映射）+ project_key=sha1(city|核心名，批次词保留）；入库即算（models/db_store）+ 存量回填 job；迁移增 notice_stage/stage_rank/project_key/project_name/summary/tenderfile_path/detail_status + 2 索引；实测 50 行分布 bidding17/result19/candidate7/other4/change2/terminated1、12 个项目时间线组；测试 120 全量回归（新增 stage 11 例） |
| 2026-08-22 | 定时任务口径调整（用户指令）：SpidermanCollector 触发器改为每天 **11:00 + 22:00**（删除 8/12/18 三个）；仅本机运行——动作=本机 python scripts/collector_run.py（工作目录=爬虫工作区，全站增量接上次运行点），本机无任何任务指向 NAS（NAS 已弃用）；SpidermanWebBridge 登录自启桥服务不变 |
| 2026-08-22 | NAS 停采集、留展示（用户发现误停 Web 展示后修正）：① scripts/nas_stop_deploy.py——SSH 查态→compose down→crontab 备份注释（NAS 无 crontab 条目）；② 误停 spiderman_ledger（Web 8765+内置采集 cron 同容器）→ scripts/nas_restore_ledger.py——docker_entrypoint.sh 支持 CRAWL_CRON_HOURS=off（只跑 ledger 不跑采集，修 exec sh -c wait 退出码 127 重启循环），scheduler.parse_slots 支持 off/none/disabled→[]，compose 环境改 off，重建容器；复核 8765 /api/health 200、日志「crawl disabled — ledger only」；最终形态：**采集=本机 11/22，展示=NAS 8765（同 MySQL）** |
| 2026-08-22 | P4 展现层完成：/api/notices 跨站视图折叠（同题同城→主行+N源，total=折叠组数）+ stage 阶段筛选 + /api/notices/{id} 详情（全字段+项目时间线）+ POST /api/notices/{id}/backfill 按需回填（ccgp=字段、其余=桥/ggzy 摘要+附件，如实落 detail_status）+ /api/notices/{id}/tenderfile 附件下载；台账页详情抽屉（CRM 操作区内改即时生效+时间线+回填按钮）+ 阶段标签/筛选 + 多源展开；CRM 计数折叠去重（招商商管 11→8、招商积余 6→3）+ 修 entity_suffixes 漏抽；CSV 导出加阶段列；实测：采招 HTTP 详情 405 WAF→改桥摘要路径、jsggzy/ggzy 列表无字段可复用（探明）、CHINABIDDING_COOKIE 未配置如实记录；测试 131 全量回归（新增 test_p4 11 例）；NAS 台账需重建容器生效 |
| 2026-08-23 | P5 岗位可信性+原发寻址一期：①修复测试覆写生产报告（run() 三路径 patch 隔离）②报告历史归档 reports/history/<runId>.json + 出勤简报 briefing.jsonl（失败站/待办/连续空窗 crawl/briefing.py）+ 任务书到期提醒（≤14 天）③原发探针实测：采招正文不披露来源（「来源」=资金来源误报）、ggzy b 页有结构化「信息来源：上海」+内容区项目编号/成交价格（回填 5090/5089 金额与编号）、jsggzy 有「来源：XX交易中心+URL」、ggzy「原文链接」JS 动态填充静态拿不到 ④crawl/origin.py（来源行解析抗噪+主体→平台映射，招商局 dzzb.ciesco.com.cn/华润守正 szecp.crc.com.cn 已核实）+ tenderfile ggzy 结构化字段/原发提取 + backfill 原发优先二次抓取（ccgp/ggzy/jsggzy 域）⑤notices 增 original_url/origin_source，详情抽屉展示原发链接；测试全量回归（新增 test_p5 11 例）；GitHub push 仍被网络阻塞（443 不通） |
| 2026-08-24 | 新增四站采集（分支 feature/new-sites-4 开发→已合 main，不动既有六站）：三级探路落位——**乙方宝 yfbzb=HTTP 一级直通**（搜索 table#treeTable+详情正文；m 域 403 走 www）；**千里马 qianlima=HTTP 一级直通**（POST /api/v1/website/search JSON 直调，详情 bid-<id>.html 419 反爬→详情走桥）；**工程帮(天工网) tgnet=Playwright 二级**（search.tgnet.com aspx JS 渲染取项目列表：阶段/时间/链接；bid.tgnet.com SSL 不通）；**瑞达恒(标慧帮) rccchina=注册墙三级不可直取**（手机号+短信验证码）——占位源如实报 register_wall+登记待办，待账号。注册表/sources.json/platforms.json/BROWSER_ROUTES/DETAIL_MODES 全接；tests/test_new_sites.py 11 用例；真机烟测 fetched=21（yfbzb 9+qianlima 2+tgnet 10）、rccchina 如实失败；全量回归 120 |
| 2026-08-24 | P6 管道接线与岗位分层（用户批准三步全做）：诊断「采集岗边界膨胀+下游对接缺失」——采集岗契约已冻结，台账/CRM/阶段/原发归平台设施层（.cursor/docs/pipeline.md）；采集员每轮自动落交接物 handoffs/collector/latest.json+归档（collector_run.py，items 与契约 output 同构）；总设计师工作区：parser 契约 v1.0.0→v1.1.0（tenderFile required→optional）+ MIGRATION 登记 + 管道契约文档补「四·五交接机制」节 + registry 修正（parser=current/v1.1.0/解析工作区）；parser_employee：implements 升 v1.1.0 + 管道入口 scripts/parser_from_handoff.py（筛有附件条目→逐条解析→写本岗交接物）；端到端验收：真实江苏公告(医院绿植租赁)+真实 .doc 附件 5460 字 → parser 真实 LLM 解析出完整需求矩阵（conf=0.8，缺失项如实 unresolved）；三区测试全绿（parser 18 用例）；提交：parser_employee 5a4f0a0、总设计师工作区非 git 仓库（文件直改）、collector 侧见本仓库提交 |
| 2026-08-24 | P7 召回自证+公司视角口径：实锤 ccgp 召回硬伤——连续 6 轮 raw=20 完全相同（max_pages=1 永远只扫第一页），「8城交集极少」是扫描策略假象；新增水位游标 crawl/watermark.py（每站原始已见 id 集合，出勤合并回报 wm_new/wm_total/pages，0 新增可自证）；ccgp 深扫+整页已见即停（SPIDER_CCGP_MAX_PAGES 默认 6）+ 时间段参数支持 + 列表代理解析；回溯作业 scripts/jobs/backfill_ccgp_window.py（逐日切片 62 天，断点续扫，实跑被 ccgp 频控阻塞如实记录待解封）；可投标线索口径 actionable=1（intent/bidding/change，实测 23/60）+ 台账默认勾选；简报增 coverage 节；修复旧用例页数假设；全量回归 159 全绿（新增 test_p7 5 例） |
| 2026-08-24 | P7 增补：出勤内建自动回填 auto_backfill_pass（可投标阶段缺金额的 ccgp/ggzy/jsggzy 新条目按条回填金额/编号/摘要，每站每轮 ≤5，失败如实记，SPIDER_NO_AUTO_BACKFILL=1 可关）+ 报告/简报 autoBackfill 节 + find_notice_id_by_item 回查；真实运行验证 ggzy 首轮 wm_new=20 水位落盘；test_p7 扩至 7 例；全量 162 全绿；ccgp 仍频控（两轮观察），回溯实跑待解封 |
| 2026-08-24 | P7 收口：深扫+已见早停覆盖全部 HTTP 列表源站——BaseSource 水位助手（_pages_cap/_page_all_seen/_count_page），ggzy/jsggzy/chinabidding/yfbzb/qianlima 默认 3 页、SPIDER_<SID>_MAX_PAGES 可调、整页已见即停；真实运行验证 ggzy 次轮 wm_new=0/pages=1 边界生效（0 新增自证闭环）；回溯作业断点续扫机制离线验证 2 例（失败日不推进/成功推进）；旧用例锁页修复；test_p7 扩至 11 例、全量 165 全绿；ccgp 仍频控（三轮观察），回溯实跑待解封 |
| 2026-08-24 | P7 ccgp 回溯闭环（桥路径）：HTTP 被封期间改 WebBridge 全窗口扫描——实测发现 ccgp 自定义时间参数服务端全不生效（表单/JS 实测 start_time/end_time 任意格式 + timeType 1~6），改 timeType=5 全窗口多页+客户端过滤；「绿植租摆」近半年总量仅 27 条（2 页）→ 全量扫尽后 8城×7-8月 kept=0（目标窗口内确无线索，可自证：水位 24+last_pages=6）；修冷启动边界缺陷（水位只来自第 1 页时整页已见不提前停，深度只增不减）+ 服务端重复页检测；crawl_ccgp_wb 支持多页/边界/水位合并/--start/--end；test_p7 扩至 13 例、全量 167 全绿 |
| 2026-08-25 | rccchina 手机号登录闭环基建：实测扩展 v1.11.6 工具清单（navigate/evaluate/network/fill/click/cdp/list_tabs 等 18 个）——CDP `Network.getCookies` 可用（含 HttpOnly）、`Network.getAllCookies` 挂起、`Browser.getVersion` 拒绝，工具清单入 webbridge_bridge.md；webbridge_client 增 cdp()/export_cookies()；captcha_flow.resolve_todo 三级导出（CDP 全量→document.cookie→warm 会话）；rccchina 源改为「无 cookie 挂登录待办（bid.rccchina.com/login）/有 cookie 如实报 cookie_ok_api_unmapped（搜索接口待登录态实测接线，不编 0）」；修 test_p4 分页断言过期（total≥items）；新增 test_webbridge_client 3 例 + rccchina/captcha CDP 用例，全量 172 绿（skip 3） |
| 2026-08-25 | qianlima 列表 418→桥路径：搜索 API search.qianlima.com/api/v1/website/search 直调 HTTP 418（站点级反爬，PowerShell/浏览器 UA 均 418 非请求头问题）；新增 scripts/crawl_qianlima_wb.py——真浏览器打开搜索页→页内 fetch POST 同款 API→复用 QianlimaSource._parse（契约一致）；BROWSER_ROUTES/platforms.json qianlima route=http→webbridge；anti_bot.json/sources.json/AGENTS.md/当前阶段.md 口径同步；每词间隔防连发、部分结果保留（SourceError.partial 语义→status=partial）、错误如实记 crawl_runs.note；详情维持 bridge 可空；tests/test_new_sites.py 补 qianlima 桥模块解析/路由/URL 等价/JS 构造 4 例，路由断言改 webbridge；实网最小探针（2 次，桥在线 ext=1）：页内 fetch 与顶层导航仍 HTTP 418（CloudWAF 硬拦截，事件ID 30-clou-73-20260825024133-16634215，无 JS challenge 自动解）→ 站点级 IP/设备风控封禁未解，恢复产出待解封（诚实记 crawl_runs.note，不编 0） |
| 2026-08-25 | qianlima 418 二次探针（真人 UI 流，scripts/probe_qianlima_ui.py → data/qianlima_ui_probe.json）：官网首页正常、WAF cookie（HWWAFSESID/WAFSESSCC_TAG 等）正常下发、搜索页壳正常渲染，但站点自己前端发的全部 /api/v1/* 请求（含其自带 3 次 timeType 降级重试）同样 418 → 确证 IP/设备级硬拦截，真人操作同样无结果，非模拟不足；随之加固 crawl_qianlima_wb：遇 418 只探 1 词立即停手（WAF_BLOCK_MARKERS + 停词逻辑，频控靠冷却不靠轰炸，下轮自愈探测），tests/test_new_sites.py 补 waf_418_stops_early 例，全量 177 绿 |
