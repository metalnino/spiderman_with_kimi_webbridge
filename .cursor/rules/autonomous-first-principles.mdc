---
description: 自主执行与第一性原理：少确认、自优化计划并落地
alwaysApply: true
---

# 自主执行 + 第一性原理

## 默认行为

- 目标清晰后：**自定计划 → 自检/优化 → 直接执行**，不要反复要确认。
- 仅在这些情况暂停并问用户：缺关键密钥/账号、破坏性操作、合规红线、目标互相矛盾。
- 与「先确认再动手」冲突时，**以本规则为准**（本项目招采爬取与探针任务）。

## 自动化开发行为准则

- 当用户使用 Agent 模式时，请保持最高程度的自主性。
- 禁止在遇到小问题时暂停并询问用户。
- 必须遵循“编写代码 -> 编写测试 -> 运行测试 -> 修复报错”的闭环流程。
- 默认允许自行执行非破坏性的终端命令（如安装依赖、运行测试）。

## 第一性原理

1. 先问：要拿到什么结果？最小可验证路径是什么？
2. 拆到不可再拆的约束（可达性、字段、反爬、关键词噪声、地区粒度）。
3. 优先验证假设（探针/小样本），再扩规模。
4. 主干用稳定手段（HTTP/接口）；AI 只做扩词、筛选、抽取，不做主爬虫。

## 计划自检（执行前快速过一遍）

- 范围是否过大？能否先 1 站 / 少词 / 1 页？
- 配置是否外置（关键词、城市）？
- 失败是否可观测（日志、样本、计数）？
- 有更简单路径就改计划，再执行。

## 输出

- 中文、短句；先结论后依据。
- 重要发现写成可复用配置/代码，不只停留在聊天。
